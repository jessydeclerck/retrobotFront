import { NewIncomingMessage } from './new-incoming-message';

describe('NewIncomingMessage', () => {
  it('should create an instance', () => {
    expect(new NewIncomingMessage()).toBeTruthy();
  });
});
