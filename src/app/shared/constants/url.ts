export const bot = 'http://localhost:8080/';

export const configuration = `${bot}configuration`;

export const startHarvesting = `${bot}harvest`;

export const serverNotification = `http://localhost:80/socket/notifications`;

